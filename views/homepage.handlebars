<h2>View post</h2>

{{#each post as |post| }}


<div class="card mb-2">
  <div class="card-body ms-3">
    
  <h5 class="card-title bg-primary text-light p-3"><a class='text-light' href="/post/{{id}}">{{post.title}}</a></h5>
  <div class="card-header">
    Posted by {{post.user_name}} on {{format_date post.createdAt}}
  </div>
  {{!-- <p class="card-text ps-4 pt-4">{{post.content}}</p> --}}
  {{#if post.comments.length}}
<p>{{post.comments.length}} comments</p>
{{/if}}
  </div>
  </div>
{{/each}}

{{!-- 
{{!-- homework --}}
<!-- Loop over each object in the projects array -->
{{#each posts as | post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/project/{{project.id}}">{{project.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed!
    </p>
    <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div>
{{/each}} --}}
